# ============================================================
# VARIABLES DE ENTORNO — SID Backend
# Copia este archivo como .env y rellena los valores reales.
# NUNCA subas .env al repositorio.
# ============================================================

# Base de datos (Local o Remota vía Túnel)
# Si usas túnel, DATABASE_URL debe apuntar al LOCAL_BIND_PORT (ej: 5433)
DATABASE_URL=postgresql+asyncpg://sid_user:sid_password_local@localhost:5432/sid_db

# SSH Tunnel Configuration
USE_SSH_TUNNEL=false
SSH_HOST=100.105.229.46
SSH_PORT=22
SSH_USER=db_manager
SSH_PKEY_PATH=~/.ssh/id_db_manager

# Remote DB settings
REMOTE_DB_HOST=127.0.0.1
REMOTE_DB_PORT=5432
LOCAL_BIND_PORT=5433

# JWT — Cambia esto por un string aleatorio seguro en producción
# Genera uno con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=cambia_esto_por_un_string_aleatorio_seguro_de_64_chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_HOURS=8

# App
APP_ENV=development
DEBUG=true
